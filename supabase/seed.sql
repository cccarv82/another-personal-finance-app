-- Seed default categories (system categories — inserted for every user via trigger or onboarding)
-- This seed is reference only; actual insertion happens per user in handle_new_user or onboarding flow.

-- Example categories structure (expense)
-- Moradia, Alimentação, Transporte, Saúde, Educação, Lazer, Roupas, Assinaturas, Outros
-- Example categories structure (income)
-- Salário, Freelance, Investimentos, Outros

-- For testing: insert a demo user's categories directly
-- Replace '<user-id>' with actual UUID when testing locally
